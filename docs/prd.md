# Jira Lite MVP - Product Requirements Document

**Author:** hojeong
**Date:** 2025-11-29
**Version:** 2.0
**Source:** PRD_KR_VER.md (원본 FR 번호 체계 유지)

---

## Executive Summary

팀이 AI의 도움으로 더 빠르게 이슈를 관리하고 해결할 수 있게 해주는 경량 이슈 트래킹 웹 애플리케이션입니다.

8시간 해커톤 과제로, AI-Native 개발 역량을 검증하기 위한 MVP 제품입니다. 기존 Jira의 복잡함을 덜어내고, AI 기능(요약, 제안, 자동분류, 중복탐지)을 핵심 차별화 요소로 삼아 현대적인 이슈 트래킹 경험을 제공합니다.

### What Makes This Special

**AI-Native 이슈 관리** - 단순히 이슈를 기록하는 것이 아니라, AI가 적극적으로 도와주는 경험:
- 이슈 설명을 자동 요약하여 빠른 파악
- 해결 전략을 AI가 제안
- 라벨을 자동 추천하여 분류 부담 감소
- 유사 이슈를 탐지하여 중복 방지
- 긴 댓글 스레드를 요약하여 맥락 파악

---

## Project Classification

**Technical Type:** SaaS B2B (saas_b2b)
**Domain:** General Software
**Complexity:** Medium-High (8시간 내 전체 구현)

이 프로젝트는 팀 기반 협업 도구로, 다음 특성을 가집니다:
- **Multi-tenant**: 팀 단위로 데이터 격리
- **Role-based Access**: OWNER/ADMIN/MEMBER 3단계 권한
- **Real-time Collaboration**: 칸반 보드 Drag & Drop
- **AI Integration**: LLM API 연동으로 지능형 기능 제공

---

## Success Criteria

### MVP 완성 기준 (8시간 내)

1. **기능 완성도** - 모든 FR 구현
   - 인증 (FR-001~007) 완전 동작
   - 팀/프로젝트/이슈 CRUD 완전 동작
   - AI 기능 5개 모두 동작
   - 대시보드 및 알림 동작

2. **배포 완료** - 실제 접근 가능한 URL로 서비스 운영

3. **사용자 경험** - 기본적인 UX 품질 확보
   - 로딩 상태 UI 표시
   - 에러 상태 처리
   - 모바일 반응형 지원
   - 대시보드 차트/시각화

---

## Functional Requirements

> **Note:** 원본 PRD_KR_VER.md의 FR 번호 체계를 그대로 유지합니다.

---

### 3.1 인증 (Authentication)

#### FR-001: 회원가입 (Sign Up)

사용자는 이메일/비밀번호를 통해 시스템에 회원가입할 수 있어야 한다.

**입력**

| 필드 | 조건 |
|------|------|
| `email` | 유니크, 이메일 형식, 최대 255자 |
| `password` | 6자 이상, 최대 100자 |
| `name` | 1~50자 |

**처리**
1. 이메일 중복 체크
2. User 생성
3. 생성 후 자동 로그인 또는 로그인 화면 이동

**예외**
- 이메일 중복 → 에러 표시
- 형식 오류 → 에러 표시

---

#### FR-002: 로그인/로그아웃 (Login/Logout)

사용자는 이메일/비밀번호로 로그인할 수 있어야 한다.

**입력**
- `email`
- `password`

**처리**
1. 이메일로 사용자 조회
2. 비밀번호 검증
3. 성공 시 세션/토큰 발급 (토큰 만료 시간: **24시간**)
4. 인증 필요한 페이지에 접근 가능

**예외**
- 불일치 → "이메일 또는 비밀번호가 올바르지 않습니다"

---

#### FR-003: 비밀번호 찾기/재설정

사용자는 이메일을 통해 비밀번호를 재설정할 수 있어야 한다.

**처리**
1. 사용자가 이메일 입력
2. 비밀번호 재설정 링크/토큰 생성 (만료 시간: **1시간**)
3. **실제 이메일 발송 필수**
4. 사용자가 새 비밀번호 입력 후 변경 완료

---

#### FR-004: Google OAuth 로그인

Google 계정을 사용한 SNS 로그인을 구현해야 한다.

**처리**
1. Google OAuth 인증 흐름 구현
2. 신규 사용자는 자동 회원가입 처리
3. 기존 사용자는 로그인 처리

**주의사항**
- 이메일/비밀번호 기반 로그인과 **별개의 인증 방식**으로 처리
- 동일 이메일로 일반 가입과 Google 로그인 계정 병합은 고려하지 않음

---

#### FR-005: 프로필 관리

사용자는 자신의 프로필 정보를 조회하고 수정할 수 있어야 한다.

**수정 가능 항목**

| 필드 | 조건 |
|------|------|
| `name` | 1~50자 |
| `profileImage` | 이미지 URL 또는 파일 업로드 |

---

#### FR-006: 비밀번호 변경

로그인한 사용자는 비밀번호를 변경할 수 있어야 한다.

**입력**

| 필드 | 조건 |
|------|------|
| `currentPassword` | 현재 비밀번호 |
| `newPassword` | 6자 이상, 최대 100자 |
| `confirmPassword` | newPassword와 일치 |

**참고**
- Google OAuth로만 가입한 사용자는 이 기능 비활성화

---

#### FR-007: 계정 삭제

사용자는 자신의 계정을 삭제할 수 있어야 한다.

**처리**
1. 비밀번호 재확인 (OAuth 사용자는 확인 버튼만)
2. 소유한 팀 존재 여부 확인
3. 소유한 팀이 있으면 삭제 불가 → 팀 삭제 또는 소유권 이전 안내
4. 계정 및 관련 데이터 Soft Delete 처리

---

### 3.2 팀 (Team) 기능

#### FR-010: 팀 생성 (Create Team)

로그인한 사용자는 새로운 팀을 생성할 수 있어야 한다.

**입력**

| 필드 | 조건 |
|------|------|
| `name` | 1~50자 |

**처리**
1. Team 생성 (`ownerId = 현재 사용자`)
2. TeamMember 생성 (role = OWNER)

**참고**
- 한 사용자는 **여러 팀에 소속 가능**
- 팀 역할은 **OWNER / ADMIN / MEMBER** 세 가지 존재

---

#### FR-011: 팀 정보 수정 (Update Team)

**권한**: 팀 OWNER, ADMIN

**수정 가능 항목**
- 팀 이름

---

#### FR-012: 팀 삭제 (Delete Team)

**권한**: 팀 OWNER만 가능

**처리**
- 팀 삭제 시 하위 프로젝트, 이슈, 댓글 등 **모두 Soft Delete 처리**
- 삭제된 팀은 30일간 복구 가능 (복구 기능은 선택)

**참고**
- OWNER는 팀 탈퇴 불가, 삭제만 가능

---

#### FR-013: 팀 멤버 초대 (Invite Member)

**권한**: 팀 OWNER, ADMIN

**처리 방식** (둘 중 선택)
1. 초대 이메일 입력 → 초대 정보 저장 → 해당 이메일로 가입 시 자동 팀 합류
2. 초대 이메일 입력 → 초대받은 사용자가 "초대 목록" 화면에서 승인 → 팀 합류

**초대 규칙**

| 항목 | 내용 |
|------|------|
| 초대 만료 기간 | **7일** |
| 초대 거절 기능 | 없음 (미수락 시 pending 상태 유지) |
| 재발송 | 가능 (기존 pending 초대의 만료일 갱신) |

**이메일 발송**
- **실제 이메일 발송 필수**

---

#### FR-014: 팀 멤버 조회 (View Members)

팀에 속한 사용자 목록을 조회할 수 있어야 한다.

**표시 정보**
- 멤버 이름, 이메일, 역할(OWNER/ADMIN/MEMBER), 가입일

---

#### FR-015: 팀 멤버 강제 퇴장 (Kick Member)

**권한**:
- OWNER: 모든 멤버 강제 퇴장 가능
- ADMIN: MEMBER만 강제 퇴장 가능

**처리**
- 해당 멤버를 팀에서 제거
- OWNER/ADMIN 본인은 강제 퇴장 불가

---

#### FR-016: 팀 탈퇴 (Leave Team)

**권한**: ADMIN, MEMBER만 가능 (OWNER는 탈퇴 불가, 삭제만 가능)

**처리**
- 해당 사용자를 팀에서 제거

---

#### FR-017: 팀 역할 체계 (OWNER/ADMIN/MEMBER)

팀 내 역할은 3단계로 구분된다.

**역할 정의**

| 역할 | 설명 |
|------|------|
| OWNER | 팀 생성자, 최고 권한, 팀당 1명 |
| ADMIN | 관리자 권한, 멤버 관리 가능 |
| MEMBER | 일반 멤버, 프로젝트/이슈 작업 가능 |

---

#### FR-018: 역할 변경 (Change Role)

**권한**: OWNER만 가능

**처리**
1. MEMBER → ADMIN 승격
2. ADMIN → MEMBER 강등
3. OWNER 권한 이전 (다른 ADMIN에게 OWNER 양도 시 본인은 ADMIN으로 변경)

**제한**
- OWNER는 최소 1명 유지 필수

---

#### FR-019: 팀 활동 로그 (Team Activity Log)

팀 내 주요 활동을 시간순으로 조회할 수 있어야 한다.

**기록 대상**
- 멤버 가입/탈퇴/강제 퇴장
- 역할 변경
- 프로젝트 생성/삭제/아카이브
- 팀 정보 수정

**표시 정보**
- 활동 내용, 수행자, 대상, 일시

**페이지네이션**
- 무한 스크롤 또는 페이지네이션 구현 필수

---

### 3.3 프로젝트 (Project)

#### FR-020: 프로젝트 생성 (Create Project)

사용자는 자신이 소속된 팀 내에 프로젝트를 생성할 수 있어야 한다.

**입력**

| 필드 | 필수 | 조건 |
|------|------|------|
| `name` | 필수 | 1~100자 |
| `description` | 선택 | 최대 2000자 |

**처리**
1. Project 생성 (`teamId` = 현재 팀, `ownerId` = 생성자)
2. 생성된 프로젝트는 팀 멤버 전체가 열람 가능

**제한**
- **팀당 최대 15개** 프로젝트

---

#### FR-021: 프로젝트 목록 조회 (View Projects)

사용자는 자신이 속한 팀의 프로젝트 전체를 조회할 수 있어야 한다.

**표시 정보**
- 프로젝트 이름, 설명 (요약), 이슈 개수, 생성일, 즐겨찾기 여부

**정렬**
- 즐겨찾기 프로젝트 우선 표시
- 이후 생성일 역순

---

#### FR-022: 프로젝트 상세 페이지 (Project Detail)

프로젝트 정보 + 해당 프로젝트의 이슈 목록/칸반 보드 표시.

**표시 정보**
- 프로젝트 이름, 설명
- 이슈 통계 (상태별 개수)
- 칸반 보드 또는 이슈 리스트 (탭 전환)

---

#### FR-023: 프로젝트 수정 (Update Project)

**권한**: 팀 OWNER, ADMIN, 또는 프로젝트 소유자

**수정 가능 항목**
- 프로젝트 이름
- 프로젝트 설명

---

#### FR-024: 프로젝트 삭제 (Delete Project)

**권한**: 팀 OWNER, ADMIN, 또는 프로젝트 소유자

**처리**
- 프로젝트 삭제 시 하위 이슈, 댓글 등 **모두 Soft Delete 처리**

---

#### FR-025: 프로젝트 설명 (Project Description)

프로젝트는 설명 필드를 가진다.

**조건**
- 최대 2000자
- 마크다운 지원 권장

---

#### FR-026: 프로젝트 아카이브 (Archive Project)

**권한**: 팀 OWNER, ADMIN, 또는 프로젝트 소유자

**처리**
1. 프로젝트를 아카이브 상태로 변경
2. 아카이브된 프로젝트는 목록에서 별도 표시 또는 숨김
3. 아카이브된 프로젝트의 이슈는 읽기 전용

**복원**
- 아카이브된 프로젝트는 언제든 복원 가능

---

#### FR-027: 프로젝트 즐겨찾기 (Favorite Project)

사용자는 프로젝트를 즐겨찾기할 수 있어야 한다.

**처리**
1. 즐겨찾기 토글 (추가/해제)
2. 즐겨찾기한 프로젝트는 목록 상단에 표시

**참고**
- 즐겨찾기는 사용자별로 관리됨

---

### 3.4 이슈 (Issue)

#### FR-030: 이슈 생성 (Create Issue)

프로젝트 안에서 이슈를 생성할 수 있어야 한다.

**입력**

| 필드 | 필수 | 조건 |
|------|------|------|
| `title` | 필수 | 1~200자 |
| `description` | 선택 | 최대 5000자 |
| `assigneeUserId` | 선택 | 같은 팀 멤버여야 함 |
| `dueDate` | 선택 | 날짜 형식 |
| `priority` | 선택 | HIGH/MEDIUM/LOW (기본: MEDIUM) |
| `labels` | 선택 | 프로젝트 라벨 중 복수 선택 |

**처리**
- 생성 시 `status = Backlog`
- 이슈 소유자 = 생성한 사람

**제한**
- **프로젝트당 최대 200개** 이슈

---

#### FR-031: 이슈 상세 조회 (Issue Detail)

다음 정보가 표시되어야 한다:
- 제목, 설명, 상태, 우선순위, 담당자, 마감일
- 라벨, 생성일
- 서브태스크 목록
- 댓글 리스트
- AI 요약 (버튼)
- AI 제안 (버튼)
- 변경 히스토리 (버튼 또는 탭)

---

#### FR-032: 이슈 수정 (Update Issue)

**권한**: 팀 멤버 전체

**수정 가능 항목**
- 제목, 설명, 담당자, 마감일, 상태, 우선순위, 라벨

---

#### FR-033: 이슈 상태 변경 (Update Status)

Drag & Drop 또는 상세 화면에서 상태 변경 가능.

**기본 상태**
- `Backlog`
- `In Progress`
- `Done`

**커스텀 상태**
- 프로젝트별로 추가 상태 정의 가능 (FR-053 참조)

**상태 전이 규칙**
- 모든 상태 간 **직접 이동 가능** (순차적 제한 없음)

---

#### FR-034: 담당자 지정 (Assign User)

`assigneeUserId`는 해당 프로젝트의 **팀 멤버 중 한 명**이어야 한다.

---

#### FR-035: 이슈 삭제 (Delete Issue)

**권한**: 이슈 소유자(생성자), 프로젝트 소유자, 팀 OWNER, 팀 ADMIN

**처리**
- Soft Delete 처리

---

#### FR-036: 이슈 검색/필터링

**검색**
- 제목 텍스트 검색

**필터**
- 상태별 (Backlog / In Progress / Done / 커스텀)
- 담당자별
- 우선순위별 (HIGH / MEDIUM / LOW)
- 라벨별
- 마감일 유무
- 마감일 범위

**정렬**
- 생성일, 마감일, 우선순위, 최근 수정일

---

#### FR-037: 이슈 우선순위 (Priority)

이슈는 우선순위를 가진다.

**우선순위 레벨**

| 레벨 | 설명 |
|------|------|
| HIGH | 긴급, 즉시 처리 필요 |
| MEDIUM | 일반적인 우선순위 (기본값) |
| LOW | 낮은 우선순위 |

**표시**
- 칸반 보드 및 이슈 목록에서 시각적 구분 (색상, 아이콘 등)

---

#### FR-038: 이슈 라벨/태그 (Labels)

프로젝트별로 커스텀 라벨을 생성하고 이슈에 적용할 수 있어야 한다.

**라벨 관리**

| 필드 | 조건 |
|------|------|
| `name` | 1~30자 |
| `color` | HEX 색상 코드 |

**처리**
1. 프로젝트 내에서 라벨 생성/수정/삭제
2. 이슈에 라벨 복수 적용 가능

**제한**
- **프로젝트당 최대 20개** 라벨
- **이슈당 최대 5개** 라벨 적용

---

#### FR-039: 이슈 변경 히스토리 (Issue History)

이슈의 변경 이력을 조회할 수 있어야 한다.

**기록 대상**
- 상태 변경, 담당자 변경, 우선순위 변경, 제목 변경, 마감일 변경

**표시 정보**
- 변경 항목, 이전 값, 새 값, 변경자, 변경 일시

---

#### FR-039-2: 서브태스크 (Sub-task)

이슈 하위에 체크리스트 형태의 서브태스크를 추가할 수 있어야 한다.

**입력**

| 필드 | 조건 |
|------|------|
| `title` | 1~200자 |

**처리**
1. 이슈 상세 화면에서 서브태스크 추가
2. 체크박스로 완료/미완료 표시
3. 드래그로 순서 변경 가능

**제한**
- **이슈당 최대 20개** 서브태스크

**표시**
- 이슈 카드에 서브태스크 진행률 표시 (예: 3/5)

---

### 3.5 AI 기능

#### FR-040: 설명 요약 생성 (AI Summary)

이슈의 description을 LLM API에 전달해 2~4문장 요약을 생성해야 한다.

**동작 방식**
- **버튼 클릭 시** 생성 (자동 생성 아님)

**캐싱**
- 한 번 생성된 AI 결과는 **저장(캐싱)**
- description이 수정되면 **캐시 무효화** (재생성 필요)

**제한**
- description이 **10자 이하**이면 AI 기능 실행 불가

**에러 처리**
- AI API 호출 실패 시 에러 메시지 표시

---

#### FR-041: 해결 전략 제안 (AI Suggestion)

"이 이슈를 해결하기 위한 접근 방식을 제안해줘"와 같은 요청을 LLM에 보내고 결과를 표시해야 한다.

**동작 방식**
- **버튼 클릭 시** 생성

**캐싱**
- FR-040과 동일하게 캐싱 및 무효화 처리

**제한**
- description이 **10자 이하**이면 실행 불가

---

#### FR-042: AI Rate Limiting

AI API 호출에 대한 rate limiting을 구현해야 한다.

**정책**
- 사용자당 분당 10회 제한
- 또는 일당 100회 제한
- 둘 중 하나 이상 구현 필수

**처리**
- 제한 초과 시 에러 메시지 및 남은 시간/횟수 안내

---

#### FR-043: AI 이슈 자동 분류 (AI Auto-Label)

이슈 생성 시 제목과 설명을 기반으로 적절한 라벨을 자동 추천해야 한다.

**동작 방식**
1. 이슈 생성 시 "AI 라벨 추천" 버튼 표시
2. 클릭 시 LLM이 프로젝트 내 기존 라벨 중 적합한 것 추천
3. 사용자가 추천 라벨 수락/거부 선택

**입력**
- 이슈 제목, 설명
- 프로젝트 내 라벨 목록

**출력**
- 추천 라벨 (최대 3개)

---

#### FR-044: AI 중복 이슈 탐지 (AI Duplicate Detection)

새 이슈 생성 시 기존 이슈와 유사한 이슈를 탐지하여 경고해야 한다.

**동작 방식**
1. 이슈 생성 폼에서 제목 입력 완료 시 (또는 버튼 클릭)
2. 기존 이슈들과 유사도 비교
3. 유사한 이슈가 있으면 경고 표시

**출력**
- 유사 이슈 목록 (최대 3개)
- 각 이슈로 이동 가능한 링크

**처리**
- 사용자가 경고를 무시하고 생성 가능

---

#### FR-045: AI 댓글 요약 (AI Comment Summary)

댓글이 많은 이슈에서 논의 내용을 요약해야 한다.

**동작 방식**
- **버튼 클릭 시** 생성
- 댓글이 **5개 이상**일 때만 버튼 활성화

**캐싱**
- 새 댓글 추가 시 **캐시 무효화**

**출력**
- 논의 요약 (3~5문장)
- 주요 결정 사항 (있는 경우)

---

### 3.6 칸반 (Kanban) 보드

#### FR-050: 칸반 보드 표시

상태별로 컬럼을 나누어 이슈를 표시한다.

**기본 컬럼**
- Backlog / In Progress / Done

**커스텀 컬럼**
- 사용자 정의 상태가 있으면 해당 컬럼도 표시

**이슈 카드 표시 정보**
- 제목, 담당자, 우선순위 (시각적 표시)
- 라벨, 마감일, 서브태스크 진행률, 생성일

---

#### FR-051: Drag & Drop 이동

이슈 카드를 다른 컬럼으로 드래그하여 상태를 변경할 수 있어야 한다.

---

#### FR-052: 같은 컬럼 내 순서 변경

같은 상태(컬럼) 내에서 이슈 순서를 위아래로 드래그하여 변경할 수 있어야 한다.

**처리**
- 순서 정보 저장 (position/order 필드)
- 새 이슈는 컬럼 최하단에 추가

---

#### FR-053: 커스텀 컬럼 (Custom Status)

프로젝트별로 기본 상태 외에 추가 상태를 정의할 수 있어야 한다.

**입력**

| 필드 | 조건 |
|------|------|
| `name` | 1~30자 |
| `color` | HEX 색상 코드 (선택) |
| `position` | 컬럼 순서 |

**제한**
- 기본 3개 + **최대 5개** 커스텀 상태 (총 8개)

**처리**
1. 커스텀 상태 생성/수정/삭제
2. 상태 삭제 시 해당 상태의 이슈는 Backlog로 이동

---

#### FR-054: WIP Limit (Work In Progress Limit)

컬럼별로 최대 이슈 개수를 제한할 수 있어야 한다.

**입력**

| 필드 | 조건 |
|------|------|
| `wipLimit` | 1~50 또는 무제한(null) |

**처리**
1. WIP Limit 설정/해제
2. 제한 초과 시 경고 표시 (이동은 허용하되 시각적 경고)

**표시**
- 컬럼 헤더에 현재 이슈 수 / WIP Limit 표시 (예: 5/10)
- 초과 시 컬럼 헤더 강조 (색상 변경 등)

---

### 3.7 댓글 (Comment)

#### FR-060: 댓글 작성 (Create Comment)

이슈 상세 화면에서 댓글을 작성할 수 있어야 한다.

**입력**

| 필드 | 조건 |
|------|------|
| `content` | 1~1000자 |

---

#### FR-061: 댓글 조회 (Comment List)

댓글은 **작성일 순**으로 표시.

**페이지네이션**
- 무한 스크롤 또는 페이지네이션 구현 필수

---

#### FR-062: 댓글 수정 (Update Comment)

**권한**: 댓글 작성자만

---

#### FR-063: 댓글 삭제 (Delete Comment)

**권한**: 댓글 작성자, 이슈 소유자, 프로젝트 소유자, 팀 OWNER, 팀 ADMIN

---

### 3.8 대시보드/통계

#### FR-080: 프로젝트 대시보드 (Project Dashboard)

프로젝트 현황을 한눈에 파악할 수 있는 대시보드를 제공해야 한다.

**표시 정보**
- 상태별 이슈 개수 (파이 차트 또는 바 차트)
- 완료율 (Done / 전체)
- 우선순위별 이슈 개수
- 최근 생성된 이슈 (최대 5개)
- 마감 임박 이슈 (7일 이내, 최대 5개)

---

#### FR-081: 개인 대시보드 (Personal Dashboard)

사용자 개인의 작업 현황을 보여주는 대시보드를 제공해야 한다.

**표시 정보**
- 내가 담당한 이슈 목록 (상태별 분류)
- 내가 담당한 이슈 총 개수
- 마감 임박 이슈 (7일 이내)
- 오늘 마감 이슈
- 최근 내가 작성한 댓글 (최대 5개)
- 소속 팀/프로젝트 목록

---

#### FR-082: 팀 통계 (Team Statistics)

팀의 전체 활동 현황을 통계로 제공해야 한다.

**표시 정보**
- 기간별 이슈 생성 추이 (꺾은선 그래프)
- 기간별 이슈 완료 추이 (꺾은선 그래프)
- 멤버별 담당 이슈 수
- 멤버별 완료 이슈 수
- 프로젝트별 이슈 현황

**기간 선택**
- 최근 7일 / 30일 / 90일

---

### 3.9 알림 (Notification)

#### FR-090: 인앱 알림 (In-App Notification)

사용자에게 중요한 이벤트를 알림으로 제공해야 한다.

**알림 발생 조건**

| 이벤트 | 알림 대상 |
|--------|----------|
| 이슈 담당자 지정 | 담당자 |
| 이슈에 댓글 작성 | 이슈 소유자, 담당자 |
| 마감일 임박 (1일 전) | 담당자 |
| 마감일 당일 | 담당자 |
| 팀 초대 | 초대 대상자 |
| 멤버 역할 변경 | 해당 멤버 |

**표시**
- 헤더에 알림 아이콘 + 미읽음 개수 표시
- 알림 목록 드롭다운 또는 페이지

---

#### FR-091: 알림 읽음 처리 (Mark as Read)

**기능**
1. 개별 알림 읽음 처리 (클릭 시)
2. 전체 읽음 처리 버튼

**표시**
- 읽음/미읽음 시각적 구분

---

### 3.10 권한/보안

#### FR-070: 팀 멤버십 검증

모든 API 엔드포인트에서 **팀 멤버십 검증 필수**.

**접근 제어**
- 다른 팀의 프로젝트/이슈에 접근 시도 시 **404 Not Found** 반환
- 권한 없는 작업 시도 시 **403 Forbidden** 반환

---

#### FR-071: Soft Delete 구현

모든 주요 엔티티에 Soft Delete를 적용해야 한다.

**적용 대상**
- User, Team, Project, Issue, Comment

**구현**
- `deletedAt` 필드 추가
- 삭제 시 물리 삭제가 아닌 `deletedAt` 타임스탬프 기록
- 조회 시 `deletedAt`이 null인 항목만 반환

---

## Non-Functional Requirements

### Performance

- 페이지 로드 시간: 3초 이내 (초기 로드)
- API 응답 시간: 500ms 이내 (일반 CRUD)
- AI API 응답 시간: 10초 이내 (LLM 응답 대기)
- Drag & Drop 반응: 즉각적 (100ms 이내)

### Security

**인증 보안:**
- 비밀번호 해싱 (bcrypt 또는 동등)
- JWT 토큰 서명 검증
- HTTPS 필수 (배포 환경)

**접근 제어:**
- 팀 멤버십 검증 필수 (모든 API)
- 권한 없는 접근: 403 Forbidden
- 존재하지 않는 리소스: 404 Not Found

**데이터 보호:**
- Soft Delete 패턴 (복구 가능)
- 민감 정보 로깅 금지

### Accessibility

- 기본적인 키보드 네비게이션 지원
- 적절한 색상 대비
- 로딩/에러 상태 명확히 표시

### Integration

**필수 연동:**
- Google OAuth (인증)
- LLM API (AI 기능): Claude, GPT, Gemini 중 선택
- 이메일 서비스: SendGrid, AWS SES, Nodemailer 등

**기술 스택 자유:**
- Frontend: React, Vue, Next.js, Svelte 등
- Backend: Express, FastAPI, Next.js API Routes 등
- Database: SQLite, Supabase, PlanetScale 등
- 배포: Vercel, Netlify, Render, Fly.io 등

---

## Data Constraints Summary

| 항목 | 제한 |
|------|------|
| 팀당 프로젝트 수 | 최대 15개 |
| 프로젝트당 이슈 수 | 최대 200개 |
| 이슈당 서브태스크 수 | 최대 20개 |
| 프로젝트당 라벨 수 | 최대 20개 |
| 이슈당 라벨 수 | 최대 5개 |
| 프로젝트당 커스텀 상태 수 | 최대 5개 (총 8개) |
| 컬럼당 WIP Limit | 1~50 또는 무제한 |
| 팀 이름 | 1~50자 |
| 프로젝트 이름 | 1~100자 |
| 프로젝트 설명 | 최대 2000자 |
| 이슈 제목 | 1~200자 |
| 이슈 설명 | 최대 5000자 |
| 서브태스크 제목 | 1~200자 |
| 라벨 이름 | 1~30자 |
| 커스텀 상태 이름 | 1~30자 |
| 댓글 내용 | 1~1000자 |
| 사용자 이름 | 1~50자 |
| 이메일 | 최대 255자 |
| 비밀번호 | 6~100자 |
| 토큰 만료 | 24시간 |
| 비밀번호 재설정 링크 만료 | 1시간 |
| 팀 초대 만료 | 7일 |
| AI 기능 최소 description 길이 | 10자 초과 |
| AI 댓글 요약 최소 댓글 수 | 5개 이상 |
| AI Rate Limit (분당) | 10회 |
| AI Rate Limit (일당) | 100회 |

---

## FR Tracking Summary

| 영역 | FR 범위 | 개수 |
|------|---------|------|
| 인증 | FR-001 ~ FR-007 | 7개 |
| 팀 | FR-010 ~ FR-019 | 10개 |
| 프로젝트 | FR-020 ~ FR-027 | 8개 |
| 이슈 | FR-030 ~ FR-039-2 | 11개 |
| AI | FR-040 ~ FR-045 | 6개 |
| 칸반 | FR-050 ~ FR-054 | 5개 |
| 댓글 | FR-060 ~ FR-063 | 4개 |
| 권한/보안 | FR-070 ~ FR-071 | 2개 |
| 대시보드 | FR-080 ~ FR-082 | 3개 |
| 알림 | FR-090 ~ FR-091 | 2개 |
| **총계** | | **58개** |

---

_This PRD captures the complete Jira Lite MVP - AI가 함께하는 스마트한 이슈 관리_

_Version 2.0 - Full FR coverage with original numbering from PRD_KR_VER.md_
